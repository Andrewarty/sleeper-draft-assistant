name: Update Sleeper Draft State

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - draft_state.json
      - recommendation.json
  schedule:
    - cron: '*/5 * * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Pull Sleeper draft state
        run: python sleeper.py
      - name: Build live recommendation
        run: python recommend.py
      - name: Commit updated state and recommendation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add draft_state.json recommendation.json
          if git diff --cached --quiet; then
            echo "No draft changes"
          else
            git commit -m "Update live Sleeper draft state and recommendation"
            git pull --rebase origin main
            git push origin main
          fi
